{
  "name": "deploy-relayhub",
  "version": "1.0.0",
  "description": "The `data.js` is populated from info from `../truffle-migrate` because that is what we actually used to deploy.",
  "main": "data.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "dotenv": "^8.2.0"
  }
}
